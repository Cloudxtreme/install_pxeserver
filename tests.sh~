#!/bin/bash
#######
# 
# What this dingus does - general purpose service setup, first specific use is install-pxe-server
#     * Define target array, directories, packages, affected files
#     * Check if some packages are installed
#     * Make copies of existing configs
#     * Change configs as necessary, add 'altered by $SCRIPT_NAME at $DATESTAMP
#     * Set up data directories (per-target)
#     * Populate data directories per target
#     * Start services
#     * Test what we can locally

#funtion defs

#function check-pkg-present ($package, $pkg-installed)
#check to see if a package is installed, write to global (hurp, bash)
		if (apt-cache policy $package) != 0; 
			then $pkg-installed = "false";
		else $pkg-installed = "true";
		fi
#or just:
		return $(apt-cache show $package)
				
# function grab-old-configs ($package, [file0, file1...]) {

	fi
		while $file[@] > len($file) {
			cp $file $script-path/}
			
